apiVersion: v1
kind: Service
metadata:
  name: {{ include "notifications-db.fullname" . }}
  labels:
    {{- include "notifications-db.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgresql
  selector:
    {{- include "notifications-db.selectorLabels" . | nindent 4 }}
