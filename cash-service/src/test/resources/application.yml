spring:
  cloud:
    config:
      enabled: false
    contract:
      stubrunner:
        ids: com.bank:accounts-service:${project.version}:stubs:8081
        stubs-mode: local
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.client.servlet.OAuth2ClientAutoConfiguration

eureka:
  client:
    enabled: false
