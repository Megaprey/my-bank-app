apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "front-ui.fullname" . }}-test"
  labels:
    {{- include "front-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget-test
      image: busybox:1.36
      command: ['wget', '--spider', '--timeout=5', 'http://{{ include "front-ui.fullname" . }}:8080/actuator/health']
  restartPolicy: Never
