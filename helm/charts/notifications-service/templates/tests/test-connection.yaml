apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "notifications-service.fullname" . }}-test"
  labels:
    {{- include "notifications-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget-test
      image: busybox:1.36
      command: ['wget', '--spider', '--timeout=5', 'http://{{ include "notifications-service.fullname" . }}:8084/actuator/health']
  restartPolicy: Never
