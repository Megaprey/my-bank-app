apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transfer-service.fullname" . }}-test"
  labels:
    {{- include "transfer-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget-test
      image: busybox:1.36
      command: ['wget', '--spider', '--timeout=5', 'http://{{ include "transfer-service.fullname" . }}:8083/actuator/health']
  restartPolicy: Never
